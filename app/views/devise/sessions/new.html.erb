<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

<div style="background-color: #666666;" class="container-table">
  <div class="vertical-center-row">

    <div class="container">
      <div id="loginbox" style="margin-top:50px;" class="mainbox col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
        <div class="panel panel-info" >
          <div class="panel-heading">
            <div class="panel-title">Вхід в портал</div>
          </div>

          <div style="padding-top:30px" class="panel-body" >

            <% unless alert.blank? %>
              <div class="alert alert-warning" role="alert"><%= alert %></div>
            <% end %>

            <div style="display:none" id="login-alert" class="alert alert-danger col-sm-12"></div>

            <form id="loginform" class="form-horizontal" role="form">

              <div style="margin-bottom: 25px" class="input-group">
                <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
                <%= f.email_field :email, class: 'form-control', autofocus: true, placeholder: "Введіть email" %>
              </div>

              <div style="margin-bottom: 25px" class="input-group">
                <span class="input-group-addon"><i class="glyphicon glyphicon-lock"></i></span>
                <%= f.password_field :password, class: 'form-control', autocomplete: "off", placeholder: "Введіть пароль"%>
              </div>

              <div class="form-inline">
                <%= f.submit "Натисність для входу", class: 'btn btn-primary' %>
                <% if devise_mapping.rememberable? -%>

                  <div class="input-group" style="margin-left: 25px">
                    <div class="checkbox">
                      <%= f.check_box :remember_me  %>
                      <%= f.label :remember_me, "Запам'ятати користувача" %>
                    </div>

                  </div>
                <% end %>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

  </div>
</div>

<% end %>


